//descriptiondata.js

function DescriptionData(in_text, in_value)
{
	//DEBUG if ( !(this instanceof DescriptionData) )
	//DEBUG {
	//DEBUG 	alert("DescriptionData: call constuctor with new keyword");	
	//DEBUG }

	this.m_text = in_text;
	this.m_value = in_value;
}

//-- END // End Concatinate, unit test or other follows
